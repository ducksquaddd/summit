module.exports = function (transaction, changes) {
	let data = transaction.params.data;
	let publisher = transaction.sender;
	let date = transaction.params.date;
	let sources = transaction.params.sources;
};
